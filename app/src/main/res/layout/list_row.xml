<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/list_row_selector"
    android:padding="8dp" >

    <!-- Nama Barang -->
    <TextView
        android:id="@+id/nama_barang"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/nama_barang"
        android:textStyle="bold" />

    <!-- Kode Barang -->
    <TextView
        android:id="@+id/kode_barang"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/nama_barang"
        android:layout_marginTop="1dip"
        android:textSize="@dimen/kode_barang" />

    <!-- Quantity -->
    <TextView
        android:id="@+id/qty"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/kode_barang"
        android:layout_marginTop="5dp"
        android:textColor="@color/qty"
        android:textSize="@dimen/qty" />

    <!-- Satuan Pack -->
    <TextView
        android:id="@+id/satuan_pack"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:textColor="@color/qty"
        android:textSize="@dimen/satuan" />

</RelativeLayout>